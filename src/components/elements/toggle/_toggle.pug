mixin toggle({label, checked}={})
  - mixinCount++;
  .toggle 
    |
    |
    label.toggle__label(for=`toggle-el-${mixinCount}`)
      input.toggle__input(
        type="checkbox",
        name=`toggle-el-${mixinCount}`,
        id=`toggle-el-${mixinCount}`,
        checked=checked
      )
      span.toggle__slider
      span.toggle__label-text= label
