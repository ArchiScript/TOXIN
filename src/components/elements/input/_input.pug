mixin input(type, label, placehld,size)
  - mixinCount++;
  - if (!label) label == "";

  div(class=`input-container --${size}-size`)
    label(for=inputId)= label
    br
    - let inputTypeClass = `input-container__${type.toLowerCase()}-input`;
    input(
      type=type,
      placeholder=placehld,
      name=`${type.toLowerCase()}-input`,
      class=inputTypeClass,
      id=`input-el-${mixinCount}`
    )
